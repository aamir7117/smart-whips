<h1>Car#index</h1>
<p>Find me in app/views/car/index.html.erb</p>
<p>hello world</p>

<% @cars.each do |car| %>
<ul>
<li> <%= image_tag car.image_url, width: 200%> <%= link_to car.model, car_path(car) %> <%= car.description %> <%= car.location %> $<%= car.price %> per day </li>
</ul>
<% end %>
