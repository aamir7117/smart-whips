<!-- restaurant menu / checkout-->
<div class="container">
  <div class="row justify-content-center">
    <title>Bootstrap Utility Classes</title>
    <link rel="shortcut icon" href="data:image/x-icon;" type="image/x-icon">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.1.2/css/all.css">
  </head>
  <body class="bg-light">
    <!-- restaurant menu / checkout-->
    <div class="container">
      <div class="container">
        <div class="row py-3">
          <div class="col-md-6">
            <h3 style="display: flex; justify-content: center;"><em>Search Results</em></h3>
            <div class="form-inline">
              <select class="search-filter col-3">
                  <option value="" disabled selected>City</option>
                  <option>London</option>
                  <option>San Francisco</option>
              </select>
              <input type="date" id="start" name="trip-start"
                        value="2023-02-28"
                        min="2023-01-01" max="2023-12-31" class="search-filter col-3">
              <select class="search-filter col-3">
                  <option value="" disabled selected>Car Type</option>
                  <option>4x4</option>
                  <option>Sedan</option>
                  <option>MiniVan</option>
                  <option>Sports Car</option>
              </select>
              <button type="submit" class="btn btn-secondary col-0.5 button"><label><i class="fa fa-search"></i> Search</label></button>
            </div>
            <% @cars.each do |car| %>
            <div class="search-card p-3 my-3 search-card">
              <div class="col-50">
                <%=cl_image_tag car.photo.key, width: 200 if car.photo.key%>
                <div class="card">
                  <p><strong><%= link_to car.model, car_path(car) %></strong></p>
                  <p>⭐⭐⭐⭐⭐</p>
                  <p><small><%= car.description %></small></p>
                  <span><strong>£<%= car.price%>/day</strong></span>
                </div>
              </div>
            </div>
            <% end %>
            <!-- /menu -->
          </div>
            <!-- checkout -->
            <div style="width: 100%; height: 600px;"
              data-controller="map"
              data-map-markers-value="<%= @markers.to_json %>"
              data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
            </div>
            <!-- /checkout -->
        </div>
        <!-- /menu -->
      </div>
    </div>
  </div>
</div>
